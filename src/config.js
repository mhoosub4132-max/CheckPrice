// ==========================================================
// ตั้งค่าสูตรคำนวณราคาตรงนี้ทั้งหมด แก้ตรงนี้ที่เดียวพอ
// ==========================================================

module.exports = {
  // โทเคนบอท (ใส่ใน .env ไม่ใช่ในไฟล์นี้ เพื่อความปลอดภัย)
  // DISCORD_TOKEN=xxxxx ใน .env

  // ---- ตารางราคาร้าน (กรณีลูกค้ามีไนโตร) ----
  // แมปแบบตรงตัว: ราคาดิสคอร์ด (บาท) -> ราคาร้าน (บาท)
  // ถ้าราคาที่อ่านได้จากรูปไม่ตรงกับตารางนี้เป๊ะ ระบบจะหาค่าที่ใกล้เคียงที่สุดให้อัตโนมัติ
  priceTable: {
    209: 49,
    229: 60,
    250: 65,
    295: 70,
    339: 95,
    359: 100,
    380: 105,
    425: 130,
    440: 145,
    459: 170,
    475: 175,
    490: 180,
    510: 210,
    539: 220,
    589: 245,
    689: 380,
  },

  // ---- ส่วนต่างราคาถ้าลูกค้าไม่มีไนโตร (บวกเพิ่มต่อชิ้น) ----
  noNitroSurchargePerItem: 30,

  // ---- จำกัดห้องที่บอทจะทำงาน ----
  // ปล่อยว่างทั้งคู่ ([]) = ทำงานได้ทุกห้องในเซิร์ฟเวอร์ (ยกเว้นห้องที่ติด blacklist ด้านล่าง)
  // ใส่ channelId ถ้าอยากให้ทำงานเฉพาะบางห้อง
  // ใส่ categoryId ถ้าอยากให้ทำงานได้ทุกห้องที่อยู่ในหมวดหมู่นั้น (เช่น หมวด "สั่งซื้อ")
  // วิธีหา ID: เปิด Developer Mode ใน Discord (User Settings > Advanced) แล้วคลิกขวาที่ห้อง/หมวดหมู่ > Copy ID
  allowedChannelIds: [
     "1522181957163679784",
  ],
  allowedCategoryIds: [
     "",
  ],

  // ---- ห้อง/หมวดหมู่ที่ห้ามบอททำงานเด็ดขาด (blacklist) ----
  // เช็คก่อนเงื่อนไข allowedChannelIds/allowedCategoryIds เสมอ
  // ถ้าห้องไหนอยู่ใน blacklist บอทจะไม่ทำงานในห้องนั้น ไม่ว่าจะตั้งค่า allowlist ไว้ยังไงก็ตาม
  blacklistedChannelIds: [
     "1423535009859371038",
     "1385948051067240489",
     "1372759378633162762",
     "1434383443545948160"
  ],
  blacklistedCategoryIds: [
    // "1234567890123456789",
  ],

  // ---- Debug ----
  // ถ้า true จะแนบข้อความดิบที่ OCR อ่านได้ต่อท้าย error ตอนอ่านราคาไม่พบ
  // เปิดไว้ตอนแก้ปัญหา OCR อ่านไม่ออก แล้วปิด (false) ตอนใช้งานจริงเพื่อความสะอาดของ embed
  debugOcr: true,

  // ---- ห้องสำหรับเปิด ticket ----
  // ใส่ channelId ของห้องที่ลูกค้าต้องไปเปิด ticket เพื่อสั่งซื้อ
  // ปล่อยเป็นค่าว่าง "" ถ้ายังไม่อยากแสดงปุ่มนี้
  ticketChannelId: "1434383443545948160",

  // ---- ตั้งค่า OCR ----
  ocr: {
    // ภาษาโมเดล OCR (eng พอสำหรับตัวเลขและสัญลักษณ์เงิน)
    language: "eng",
    // regex สำหรับดึงราคาจากข้อความที่ OCR อ่านได้
    // Tesseract มักอ่านสัญลักษณ์ ฿ (บาทไทย) เป็นตัวอักษร "B" แทน
    // pattern นี้จับทั้ง "THB 209.00", "฿750.00", "B475.00" ในรูปแบบเดียวกันหมด
    // บังคับให้ต้องมีทศนิยม 2 ตำแหน่ง (.XX) เสมอ เพราะตัวเลขที่ไม่มีทศนิยม
    // มักถูก OCR อ่านเพี้ยนบ่อยกว่า (เช่น "750" กลายเป็น "58") ส่วนตัวเลขที่มี
    // ทศนิยมชัดเจนแบบ "475.00" อ่านแม่นกว่ามาก จึงใช้กรองตัวเลขขยะออกไปในตัว
    pricePattern: /[฿B]\s?([\d,]+\.\d{2})/gi,
  },
};
